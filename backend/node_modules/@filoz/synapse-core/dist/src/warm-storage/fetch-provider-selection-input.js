import { getProviderIds } from "../endorsements/get-provider-ids.js";
import { getApprovedPDPProviders } from "../sp-registry/get-pdp-providers.js";
import { getPdpDataSets } from "./get-pdp-data-sets.js";
export async function fetchProviderSelectionInput(client, options) {
    const [providers, endorsedIds, pdpDataSets] = await Promise.all([
        getApprovedPDPProviders(client),
        getProviderIds(client),
        getPdpDataSets(client, { address: options.address }),
    ]);
    return {
        providers,
        endorsedIds,
        clientDataSets: pdpDataSets,
    };
}
//# sourceMappingURL=fetch-provider-selection-input.js.map