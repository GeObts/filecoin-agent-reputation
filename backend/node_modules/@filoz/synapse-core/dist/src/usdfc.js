import { watchAsset } from 'viem/actions';
import { asChain } from "./chains.js";
export async function watchUsdfc(client) {
    const chain = asChain(client.chain);
    const token = chain.contracts.usdfc.address;
    const result = await watchAsset(client, {
        type: 'ERC20',
        options: {
            address: token,
            symbol: 'USDFC',
            decimals: 18,
            image: 'https://app.usdfc.net/apple-touch-icon.png',
        },
    });
    return result;
}
//# sourceMappingURL=usdfc.js.map